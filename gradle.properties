# Project-level Gradle properties (safe to commit - no secrets here)
# Secrets are injected via GitHub Actions environment variables at publish time.
org.gradle.jvmargs=-Xmx2g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.caching=true
